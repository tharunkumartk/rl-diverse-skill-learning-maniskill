diff --git a/examples/baselines/experimental/sac_moe/sac_moe.py b/examples/baselines/experimental/sac_moe/sac_moe.py
index 6d4f29f0..3fe275cf 100644
--- a/examples/baselines/experimental/sac_moe/sac_moe.py
+++ b/examples/baselines/experimental/sac_moe/sac_moe.py
@@ -120,6 +120,14 @@ class Args:
     bootstrap_at_done: str = "always"
     """the bootstrap method to use when a done signal is received. Can be 'always' or 'never'"""
 
+    # DIAYN expert diversity arguments
+    disc_coeff: float = 0.1
+    """weight for expert discriminability loss added to Q/V optimizer steps"""
+    intrinsic_reward_coeff: float = 0.05
+    """weight for DIAYN-style discriminator intrinsic diversity reward"""
+    disc_lr: float = 3e-4
+    """learning rate for the expert discriminators"""
+
     # to be filled in runtime
     grad_steps_per_iteration: int = 0
     """the number of gradient updates per iteration"""
@@ -233,34 +241,60 @@ class MoE(nn.Module):
         outputs = torch.stack([expert(x) for expert in self.experts], dim=-1) 
         weights = weights.unsqueeze(1).expand_as(outputs) 
         return torch.sum(outputs * weights, dim=-1)
+
+    def get_expert_representations(self, x, a=None):
+        if a is not None:
+            x = torch.cat([x, a], dim=-1)
+        return torch.stack([e.encode(x) for e in self.experts], dim=1)  # [B, K, 256]
      
 class VNetwork(nn.Module):
     def __init__(self, env):
         super().__init__()
-        self.net = nn.Sequential(
+        self.encoder = nn.Sequential(
             nn.Linear(np.array(env.single_observation_space.shape).prod(), 256),
             nn.ReLU(),
             nn.Linear(256, 256),
-            nn.ReLU(), 
+            nn.ReLU(),
             nn.Linear(256, 256),
-            nn.ReLU(), 
-            nn.Linear(256, 1)
+            nn.ReLU(),
         )
+        self.head = nn.Linear(256, 1)
 
     def forward(self, x):
-        return self.net(x)
+        return self.head(self.encoder(x))
+
+    def encode(self, x):
+        return self.encoder(x)
 
 class SoftQNetwork(nn.Module):
     def __init__(self, env):
         super().__init__()
-        self.net = nn.Sequential(
+        self.encoder = nn.Sequential(
             nn.Linear(np.array(env.single_observation_space.shape).prod() + np.prod(env.single_action_space.shape), 256),
             nn.ReLU(),
             nn.Linear(256, 256),
             nn.ReLU(),
             nn.Linear(256, 256),
             nn.ReLU(),
-            nn.Linear(256, 1),
+        )
+        self.head = nn.Linear(256, 1)
+
+    def forward(self, x):
+        return self.head(self.encoder(x))
+
+    def encode(self, x):
+        return self.encoder(x)
+
+
+class ExpertDiscriminator(nn.Module):
+    def __init__(self, repr_dim: int, num_experts: int):
+        super().__init__()
+        self.net = nn.Sequential(
+            nn.Linear(repr_dim, 128),
+            nn.ReLU(),
+            nn.Linear(128, 128),
+            nn.ReLU(),
+            nn.Linear(128, num_experts),
         )
 
     def forward(self, x):
@@ -422,6 +456,16 @@ if __name__ == "__main__":
     actor_optimizer = optim.Adam(list(actor.parameters()), lr=args.policy_lr)
     predictor_optimizer = optim.Adam(list(value_predictor.parameters()), lr=args.q_lr)
 
+    num_experts = 4
+    repr_dim = 256
+    disc_qf1 = ExpertDiscriminator(repr_dim, num_experts).to(device)
+    disc_qf2 = ExpertDiscriminator(repr_dim, num_experts).to(device)
+    disc_vp = ExpertDiscriminator(repr_dim, num_experts).to(device)
+    disc_q_optimizer = optim.Adam(
+        list(disc_qf1.parameters()) + list(disc_qf2.parameters()), lr=args.disc_lr
+    )
+    disc_vp_optimizer = optim.Adam(list(disc_vp.parameters()), lr=args.disc_lr)
+
     # Automatic entropy tuning
     if args.autotune:
         target_entropy = -torch.prod(torch.Tensor(envs.single_action_space.shape).to(device)).item()
@@ -556,21 +600,69 @@ if __name__ == "__main__":
                 target_q_values = torch.min(qf1_next_target, qf2_next_target) 
                 target_values = 0.8 * target_q_values  + 0.2 * target_v_values
                 min_qf_next_target = target_values - alpha * next_state_log_pi
-                next_q_value = data.rewards.flatten() + (1 - data.dones.flatten()) * args.gamma * (min_qf_next_target).view(-1)
+
+                # DIAYN intrinsic reward: log q_phi(z|rep) + log K
+                reps_next = qf1_target.get_expert_representations(data.next_obs, next_state_actions)
+                B_r, K_r, D_r = reps_next.shape
+                reps_next_flat = reps_next.reshape(B_r * K_r, D_r)
+                labels_next = torch.arange(K_r, device=device).repeat(B_r)
+                log_probs_next = F.log_softmax(disc_qf1(reps_next_flat), dim=-1)
+                correct_log_prob = log_probs_next[torch.arange(B_r * K_r, device=device), labels_next]
+                correct_log_prob = correct_log_prob.reshape(B_r, K_r).mean(dim=1)
+                r_div = correct_log_prob + np.log(K_r)
+                augmented_rewards = data.rewards.flatten() + args.intrinsic_reward_coeff * r_div
+
+                next_q_value = augmented_rewards + (1 - data.dones.flatten()) * args.gamma * (min_qf_next_target).view(-1)
                 # data.dones is "stop_bootstrap", which is computed earlier according to args.bootstrap_at_done
             
             qf1_a_values = qf1(data.obs, data.actions).view(-1)
             qf2_a_values = qf2(data.obs, data.actions).view(-1)
             V = value_predictor(data.obs)
-            
+
+            # Expert representations for discriminator diversity
+            reps_q1 = qf1.get_expert_representations(data.obs, data.actions)
+            reps_q2 = qf2.get_expert_representations(data.obs, data.actions)
+            B_d, K_d, D_d = reps_q1.shape
+            reps_q1_flat = reps_q1.reshape(B_d * K_d, D_d)
+            reps_q2_flat = reps_q2.reshape(B_d * K_d, D_d)
+            expert_labels = torch.arange(K_d, device=device).repeat(B_d)
+
+            # Discriminator step: train discriminators to tell experts apart (detach reps)
+            disc_logits_q1_d = disc_qf1(reps_q1_flat.detach())
+            disc_logits_q2_d = disc_qf2(reps_q2_flat.detach())
+            disc_q_loss = (F.cross_entropy(disc_logits_q1_d, expert_labels) + F.cross_entropy(disc_logits_q2_d, expert_labels)) / 2
+            disc_q_optimizer.zero_grad()
+            disc_q_loss.backward()
+            disc_q_optimizer.step()
+
+            # Expert discriminability loss: gradients flow into expert encoders
+            disc_logits_q1_e = disc_qf1(reps_q1_flat)
+            disc_logits_q2_e = disc_qf2(reps_q2_flat)
+            expert_disc_q_loss = (F.cross_entropy(disc_logits_q1_e, expert_labels) + F.cross_entropy(disc_logits_q2_e, expert_labels)) / 2
+
             qf1_loss = F.mse_loss(qf1_a_values, next_q_value)
             qf2_loss = F.mse_loss(qf2_a_values, next_q_value)
-            qf_loss = qf1_loss + qf2_loss
+            qf_loss = qf1_loss + qf2_loss + args.disc_coeff * expert_disc_q_loss
 
             q_optimizer.zero_grad()
             qf_loss.backward()
             q_optimizer.step()
-            
+
+            # Value predictor discriminator diversity
+            reps_vp = value_predictor.get_expert_representations(data.obs)
+            B_v, K_v, D_v = reps_vp.shape
+            reps_vp_flat = reps_vp.reshape(B_v * K_v, D_v)
+            vp_labels = torch.arange(K_v, device=device).repeat(B_v)
+
+            disc_logits_vp_d = disc_vp(reps_vp_flat.detach())
+            disc_vp_loss_val = F.cross_entropy(disc_logits_vp_d, vp_labels)
+            disc_vp_optimizer.zero_grad()
+            disc_vp_loss_val.backward()
+            disc_vp_optimizer.step()
+
+            disc_logits_vp_e = disc_vp(reps_vp_flat)
+            expert_disc_vp_loss = F.cross_entropy(disc_logits_vp_e, vp_labels)
+
             with torch.no_grad():
                 qf1_a_values = qf1(data.obs, data.actions)
                 qf2_a_values = qf2(data.obs, data.actions)
@@ -579,7 +671,7 @@ if __name__ == "__main__":
             vf_err = V - Q
             vf_sign = (vf_err > 0).float()
             vf_weight = (1 - vf_sign) * 0.7 + vf_sign * 0.3
-            predictor_loss = (vf_weight * (vf_err**2)).mean()
+            predictor_loss = (vf_weight * (vf_err**2)).mean() + args.disc_coeff * expert_disc_vp_loss
             predictor_optimizer.zero_grad()
             predictor_loss.backward()
             predictor_optimizer.step()
@@ -637,6 +729,20 @@ if __name__ == "__main__":
             if args.autotune:
                 logger.add_scalar("losses/alpha_loss", alpha_loss.item(), global_step)
 
+            # DIAYN diversity metrics
+            logger.add_scalar("moe/disc_q_loss", disc_q_loss.item(), global_step)
+            logger.add_scalar("moe/disc_vp_loss", disc_vp_loss_val.item(), global_step)
+            logger.add_scalar("moe/expert_disc_q_loss", expert_disc_q_loss.item(), global_step)
+            logger.add_scalar("moe/expert_disc_vp_loss", expert_disc_vp_loss.item(), global_step)
+            logger.add_scalar("moe/intrinsic_reward_mean", r_div.mean().item(), global_step)
+            with torch.no_grad():
+                disc_acc_q1 = (disc_logits_q1_d.argmax(dim=1) == expert_labels).float().mean().item()
+                disc_acc_q2 = (disc_logits_q2_d.argmax(dim=1) == expert_labels).float().mean().item()
+                disc_acc_vp = (disc_logits_vp_d.argmax(dim=1) == vp_labels).float().mean().item()
+            logger.add_scalar("moe/disc_accuracy_q1", disc_acc_q1, global_step)
+            logger.add_scalar("moe/disc_accuracy_q2", disc_acc_q2, global_step)
+            logger.add_scalar("moe/disc_accuracy_vp", disc_acc_vp, global_step)
+
     if not args.evaluate and args.save_model:
         model_path = f"runs/{run_name}/final_ckpt.pt"
         torch.save({
